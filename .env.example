# Slack tokens — get these from https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_SIGNING_SECRET=your-signing-secret

# Authorized Slack user IDs (comma-separated)
# Find your ID: Slack profile → ⋮ → Copy member ID
# Leave empty to allow all users
AUTHORIZED_USERS=UNITYESH,UPIYUSH

# Working directory — the folder Claude runs from when invoked via Slack.
# Set to home so Luo Ji can access everything on the machine.
# Combined with passwordless sudo and --permission-mode bypassPermissions,
# Luo Ji has full root access to the entire machine — which is exactly
# what you want for an AI cofounder that needs to do everything.
PROJECT_DIR=/Users/luo

# Claude CLI timeout in seconds (default: 1800 = 30 min)
CLAUDE_TIMEOUT=1800

# HTTP server port (Cloudflare Tunnel points to this)
PORT=3000

# --- Extensions ---

# Gemini API — image generation via Imagen (Nano Banana)
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key
